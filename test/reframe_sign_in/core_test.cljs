(ns reframe-sign-in.core-test
  (:require [cljs.test :refer-macros [deftest testing is]]
            [reframe-sign-in.core :as core]))

(deftest fake-test
  (testing "fake description"
    (is (= 1 2))))
